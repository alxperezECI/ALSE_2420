#include <iostream>
#include <cmath>

using namespace std;
int main()
{
    float t, incre;
    cout << "Digite el valor de t: ";
    cin >> t;
    cout << "Digite el incremento: ";
    cin >> incre;
    float x[100]; // crear un vector de 100 posiciones
    for (int i = 0; i <= 100; i++)
    {
        x[i] = ;
        i + incre;
    }
    fx[i] = sin(2 * pi * t * x[i]);

    cout << "x\tf(x)" << endl;
    for (int i = 0; i < N; i++)
    {
        cout << x[i] << "\t" << fx[i] << endl;
    }

        return 0;
}
