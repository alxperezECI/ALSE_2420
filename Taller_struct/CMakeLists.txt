# Versión mínima requerida de CMake
cmake_minimum_required(VERSION 3.10)

# Nombre del proyecto
project(complejos)

# Agregar los archivos fuente para el ejecutable (main.cpp) y el archivo de cabecera (complex.h)
add_executable(${PROJECT_NAME} main.cpp)


