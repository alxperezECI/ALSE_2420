#include <iostream>
using namespace std;
#include <cmath>
int main(){
float x[100];
float t;
float inc;
float fx;
// Marcar los puntos de cruce por cero        
if (fx[i] <= 0 && !crossingPoints[i]) 
{            
    crossingPoints[i] = true;            
    crossingPoints[i+1] = true;       
     }    

for (float i = 0; i < 100; i++)
{
    if( inc < 1)
    {
        x[i] = i* inc;
        fx[i] = sin(2 * M_PI * t * X[i]);
    }
    cout << "No sirvo"<< endl;
}
cin >>"De un valor para t: ">> t >> endl;
cin >> "De un valor para incremento: " >> inc >> endl;
cout << "Funcion de f(x): "<<fx[i]<< endl;
if (crossingPoints[i]) 
{            
    std::cout << "*";    
    }
return 0;

}
